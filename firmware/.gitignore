.pio/
.pioenvs/
.piolibdeps/
.clangd/
.cache/

# generated by the PlatformIO IDE extension, machine-specific
.vscode/c_cpp_properties.json
.vscode/ipch/
.vscode/.browse.c_cpp.db*

*.bin
*.elf
*.map
compile_commands.json

# Per-machine Claude Code state (permissions granted on this box only).
.claude/settings.local.json

# Python helper output (tools/make_time_font.py).
__pycache__/
*.pyc

# Firmware images and other build leftovers.
*.hex
*.uf2
*.o
*.a

# OS litter.
.DS_Store
Thumbs.db

# Ad-hoc preview renders. Anchored with a leading slash on purpose: a bare
# `preview/` also matches docs/preview/ at any depth, and those two PNGs are
# checked in - README.md shows them.
/preview/
